# This will probably not be a shell script...- jkg
# There should be an intermediary server that we can POST to with rdf from here
#   or the server could be an additional application on sul-rdf-dev.stanford.edu with localhost CRUD operations.
